buildscript {
    if (project == rootProject) {
        repositories {
            google()
            jcenter()
        }

        dependencies {
            classpath 'com.android.tools.build:gradle:3.5.3'
        }
    }
}

apply plugin: 'com.android.library'

def safeExtGet(prop, fallback) {
    rootProject.ext.has(prop) ? rootProject.ext.get(prop) : fallback
}

android {
    compileSdkVersion safeExtGet('Alipay_compileSdkVersion', 29)
    buildToolsVersion safeExtGet('Alipay_buildToolsVersion', '29.0.2')
    defaultConfig {
        minSdkVersion safeExtGet('Alipay_minSdkVersion', 16)
        targetSdkVersion safeExtGet('Alipay_targetSdkVersion', 29)
        versionCode 1
        versionName "1.0"

    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
    lintOptions {
        disable 'GradleCompatible'
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }
}

repositories {
    mavenLocal()
    flatDir { dirs 'libs' }
    maven {
        // All of React Native (JS, Obj-C sources, Android binaries) is installed from npm
        url("$rootDir/../node_modules/react-native/android")
    }
    google()
    jcenter()
}

dependencies {
    //noinspection GradleDynamicVersion
    implementation "com.facebook.react:react-native:+"  // From node_modules

    compile (name: 'alipaysdk-15.8.03.210428205839', ext: 'aar')
    // utdid冲突，如果集成阿里百川，则需要用不含utdid的包
    compile (name: 'alipaysdk-noutdid-15.8.03.210428205839', ext: 'aar')
}
